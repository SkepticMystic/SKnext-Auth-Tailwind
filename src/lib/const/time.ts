const MIN = 1000 * 60;
const HOUR = MIN * 60;
const DAY = HOUR * 24;
const WEEK = DAY * 7;
const YEAR = DAY * 365;
const MONTH = YEAR / 12;

export const TIME = {
  MIN,
  HOUR,
  DAY,
  WEEK,
  MONTH,
  YEAR,
};
